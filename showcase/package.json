{
  "name": "plancia-ui-showcase",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vetrina di plancia-ui: ogni componente in ogni stato, nei due temi e nelle due densità. Legge i sorgenti del pacchetto (../src) con un alias, non dist.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^26.6.2",
    "svelte": "^5.57.1",
    "svelte-check": "^4.7.2",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
